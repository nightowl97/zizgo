# OS junk
Thumbs.db
Desktop.ini
.DS_Store

# editors
.vscode/
.idea/
*.swp

# original full-res splash source (30MB; the app uses assets/splash.webp)
assets/splash.png

# data-analysis notebook (references a local history.jsonl; stats are baked into the app)
_stats.ipynb

# logs
*.log

# Android / TWA (Bubblewrap) — never commit the signing key or build output
android.keystore
*.keystore
*.jks
local.properties
.gradle/
build/
app/build/
*.apk
*.aab
